spring:
  application:
    name: server-gateway
  profiles:
    active: dev
  main:
    allow-bean-definition-overriding: true
    web-application-type: reactive

auth:
  interceptor:
    enabled: false

server:
  ssl:
    enabled: true
    key-store: classpath:certs/dongyangyuan.xin.p12
    key-store-password: 13hunzhidang
    key-store-type: PKCS12
